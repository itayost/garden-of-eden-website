---
phase: 03-video-management
plan: 05
type: execute
wave: 4
depends_on: ["03-04"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Admin can create videos via the UI"
    - "Admin can edit existing videos"
    - "Admin can delete videos with confirmation"
    - "YouTube thumbnails display correctly"
    - "Table sorting and filtering work"
  artifacts: []
  key_links: []
---

<objective>
Human verification of complete video management functionality.

Purpose: Ensure all video CRUD operations work correctly from the user's perspective.
Output: Verified working video management system ready for production.
</objective>

<execution_context>
@/Users/itayostraich/.claude/get-shit-done/workflows/execute-plan.md
@/Users/itayostraich/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-video-management/03-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete admin video management interface with CRUD operations</what-built>
  <how-to-verify>
1. **Navigate to video management:**
   - Go to /admin/videos
   - Verify page loads with video table
   - Verify YouTube thumbnails display for existing videos

2. **Test video creation:**
   - Click "הוסף סרטון" button
   - Fill form with test data:
     - כותרת: "סרטון בדיקה"
     - YouTube URL: https://www.youtube.com/watch?v=dQw4w9WgXcQ (or any valid YouTube URL)
     - יום: Select day 1
     - נושא: Should auto-fill with "גמישות ויציבות"
     - משך: 5
     - תיאור: "תיאור לבדיקה"
   - Submit form
   - Verify success toast appears
   - Verify redirect to /admin/videos
   - Verify new video appears in table

3. **Test video editing:**
   - Find the test video in table
   - Click edit button (pencil icon)
   - Verify Sheet/Dialog opens with pre-populated data
   - Change title to "סרטון בדיקה מעודכן"
   - Save changes
   - Verify success toast appears
   - Verify table updates with new title

4. **Test table features:**
   - Click "כותרת" header to sort alphabetically
   - Click "יום" header to sort by day
   - Type in search box and verify filtering works
   - Select a day from filter dropdown and verify results

5. **Test video deletion:**
   - Click delete button (trash icon) on test video
   - Verify confirmation dialog appears with video title
   - Click "מחק סרטון"
   - Verify success toast appears
   - Verify video removed from table

6. **Test validation:**
   - Go to /admin/videos/create
   - Try submitting with empty title - verify error shown
   - Try invalid YouTube URL - verify error shown
   - Enter duration > 120 - verify error shown

7. **Test pagination (if enough videos):**
   - Verify pagination shows correct count
   - Click navigation buttons if more than 10 videos
  </how-to-verify>
  <resume-signal>Type "approved" to complete phase, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All manual tests pass without errors
</verification>

<success_criteria>
- All CRUD operations work correctly
- YouTube thumbnails load properly
- Table sorting, filtering, pagination work
- Form validation prevents invalid submissions
- Loading states and toast notifications appear appropriately
- UI is consistent with existing admin pages (Hebrew, RTL)
</success_criteria>

<output>
After completion, create `.planning/phases/03-video-management/03-05-SUMMARY.md`
</output>
