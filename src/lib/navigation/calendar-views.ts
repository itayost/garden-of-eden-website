/**
 * The calendar's two views: the dated days staff work in, and the standing
 * week those days are seeded from. Their paths and names live here once, for
 * the tabs, the pages' titles, and links into them. A plain module rather
 * than part of the client tabs, so server pages read the strings themselves.
 */
export const CALENDAR_PATH = "/admin/calendar";
export const CALENDAR_TEMPLATE_PATH = "/admin/calendar/template";

export const CALENDAR_LABEL = "יומן יומי משתנה";
export const CALENDAR_TEMPLATE_LABEL = "תכנית אימונית קבועה";
